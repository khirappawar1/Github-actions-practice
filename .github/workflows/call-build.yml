---
name: Call Resuable Build Workflow
on:
  push:
    branches: [main]
     

jobs:
    call-build:
        uses: ./.github/workflows/resuable-build.yml
        with: 
            app_name: my-app
            environment: production
        secrets:
          docker_token: ${{ secrets.DOCKER_TOKEN }}


