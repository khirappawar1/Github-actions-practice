---
name: Scheduled-job check
"on":
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 0 * * *"

jobs:
  scheduled_job:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "Workflow triggered"

