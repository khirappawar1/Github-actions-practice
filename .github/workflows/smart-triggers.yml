name: Smart Path Trigger

on:
  push:
    branches:
      - main
      - 'release/*'
    paths:
      - 'src/**'
      - 'Docker-app/**'

jobs:
  run-app-workflow:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Triggered because src/ or app/ changed"